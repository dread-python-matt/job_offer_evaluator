// Production build configuration (swapped in for environment.ts via angular.json
// fileReplacements when building with --configuration production).
//
// Set `apiUrl` to the backend's public origin, e.g. 'https://api.your-domain.com'.
// Leave it as '' (same-origin) only if the API is reverse-proxied under the same
// host as this app.
export const environment = {
  apiUrl: '',
};
